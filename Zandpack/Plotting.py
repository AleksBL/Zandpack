import numpy as np
import matplotlib.pyplot as plt
import sisl
import os


t = np.linspace(0,1)


# def make_png_series(t, mat, pos, idx_to_pos, axes = [0,1], max_R = 1.5, directory = 'png_series'):
#     try:    os.mkdir(directory)
#     except: pass
    
#     os.chdir(directory)
#     os.chdir('..')
    
    
    