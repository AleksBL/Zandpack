import numpy as np
from numba import njit

#Jit'ing is not required, but possible


@njit
def dH(t,sigma):
    out = np.zeros(sigma.shape, dtype = sigma.dtype)
    return out


# Change the bias to something more interesting for the "continue" run
@njit
def bias(t,a):
    if a == 0:
        return 0.0 
    if a == 1:
        return 0.0

